{
  "name": "control-room-android",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "android:prepare": "npm run build && node scripts/prepare-android.mjs",
    "android:sync": "npm run build && node scripts/prepare-android.mjs && npx cap sync android",
    "android:open": "npm run android:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "8.5.1",
    "@capacitor/push-notifications": "8.1.2"
  },
  "devDependencies": {
    "@capacitor/android": "8.5.1",
    "@capacitor/cli": "8.5.1",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
